spring:
  application:
    name: reap-facility
  cloud:
    config:
      enabled: false
    refresh:
      refreshable: none      
      
reap:
  config:
    enabled: false
eureka: 
  client:
    enabled: false